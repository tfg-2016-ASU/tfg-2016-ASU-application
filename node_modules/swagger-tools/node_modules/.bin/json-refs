#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../json-refs/bin/json-refs" "$@"
  ret=$?
else 
  node  "$basedir/../json-refs/bin/json-refs" "$@"
  ret=$?
fi
exit $ret
