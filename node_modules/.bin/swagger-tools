#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../swagger-tools/bin/swagger-tools" "$@"
  ret=$?
else 
  node  "$basedir/../swagger-tools/bin/swagger-tools" "$@"
  ret=$?
fi
exit $ret
